# C07A + S28A H 题电赛小车硬件资源表

> MCU: TI MSPM0G3507  
> 核心板: C07A  
> 底板: S28A  
> 视觉模块: K230 HiWonder  
> 球杆执行机构: 42 步进电机 + D36A 双路驱动模块（仅使用通道 1）
> 电池: 7.4 V

---

## 1. 硬件组成与占用原则

- 不使用：蓝牙模块、电池检测模块、原 CCD 巡线模块、手柄、云台舵机、超声波模块。
- 保留：OLED、MPU6050、板载按键 PA18、板载 LED PB9。
- 球杆执行机构固定使用：PB16 作为 STEP、PB17 作为 DIR、PA24 作为 EN。
- 蜂鸣器：PA9 GPIO 输出，高电平触发。
- 所有模块必须共地。

正式赛题与摆杆约束见 [H 题原文](.agents/skills/c07a-s28a-car/references/competition/H题_车载平衡滚球运动控制系统.pdf)。

---

## 2. 底盘电机资源

### Motor A

| 功能 | 引脚 | 外设 |
|---|---|---|
| Encoder A | PA25 | GPIO Interrupt |
| Encoder B | PA26 | GPIO Interrupt |
| Direction 1 | PA13 | GPIO Output |
| Direction 2 | PA14 | GPIO Output |
| PWM | PB2 | TIMG6_C0 |

### Motor B

| 功能 | 引脚 | 外设 |
|---|---|---|
| Encoder A | PB20 | GPIO Interrupt |
| Encoder B | PB24 | GPIO Interrupt |
| Direction 1 | PA17 | GPIO Output |
| Direction 2 | PA16 | GPIO Output |
| PWM | PB3 | TIMG6_C1 |

### 待标定硬件参数

- Motor A = 右轮（用户实车循迹转向确认）
- Motor B = 左轮（用户实车循迹转向确认）
- Motor A 正方向极性：PA14 = 高，PA13 = 低（用户实车确认）
- Motor B 正方向极性：PA16 = 高，PA17 = 低（用户实车确认）
- Encoder A/B 正转相位方向：待实测确认
- 电机型号：TBD
- 减速比：TBD
- 编码器 PPR：TBD
- 车轮直径：TBD
- 轮距：TBD

---

## 3. MPU6050

| 功能 | 引脚 | 外设 |
|---|---|---|
| SDA | PA0 | I2C |
| SCL | PA1 | I2C |
| INT | PA7 | GPIO Interrupt |

---

## 4. K230 视觉模块与 UART 物理连接

K230 开发板配套显示硬件：3.5 英寸 MIPI LCD 接口，分辨率 800×480。

### K230 → MSPM0G3507

```text
K230 40Pin 物理 8 脚 / TX1(IO3) / UART1_TXD
        ->
MSPM0 PB7 / UART1_RX
```

### MSPM0G3507 → K230

```text
MSPM0 PB6 / UART1_TX
        ->
K230 40Pin 物理 10 脚 / RX1(IO4) / UART1_RXD
```

### 公共连接

```text
K230 GND <-> S28A GND
```

来源说明：K230 板级显示接口和 40Pin TX1(IO3)/RX1(IO4) 路由来自幻尔科技 K230 开发板原理图 V1.0，仓库副本及证据边界见 [K230 资料索引](.agents/skills/c07a-s28a-car/references/module-docs/k230/index.md)。本项目采用上述 UART1 路由；先前记录的 GPIO11/GPIO12 UART2 路由已废止，参考例程中的其他 GPIO、UART 或 LED 接线不覆盖本项目连接。

---

## 5. 球杆系统传动件与 D36A

球杆系统仅使用一个 42 步进电机和 D36A 的步进电机 1/通道 1。D36A 通道 2、Motor 2 接口均不使用。

### 5.1 MSPM0 → D36A 控制接口

| MSPM0 | 外设/模式 | D36A P1 | D36A 信号 | 用途 |
|---|---|---|---|---|
| PB16 | TIMG7_C1 | 8 | ST1 / STEP1 | 步进脉冲，上升沿有效 |
| PB17 | GPIO Output | 6 | DIR1 | 电机方向 |
| PA24 | GPIO Output | 4 | EN1 | 低电平休眠，高电平唤醒 |
| S28A GND | GND | 9 或 10 | GND | 逻辑共地 |

D36A P1 其余信号保持如下：

| P1 引脚 | 信号 | 本项目处理 |
|---|---|---|
| 1 | ADC，VIN 的 1/11 | 不连接 |
| 2 | 5 V 输出 | 不连接，不得与 S28A/P03B 5 V 并联 |
| 3、5、7 | EN2、DIR2、STEP2 | 不连接 |
| 9、10 | GND | 至少连接一根到 S28A GND |

电气与时序约束：

- D36A 丝印 `EN1` 实际接至 ATD5984 的 `SLEEP#`，不是芯片的低有效 `ENABLE#`。模块定义是低电平休眠、高电平唤醒。
- ATD5984 的逻辑高电平门限为 2.8 V，MSPM0 的 3.3 V GPIO 可直接连接，无需电平转换。
- STEP 高电平和低电平持续时间均不得短于 1 µs。
- DIR 相对 STEP 有效上升沿的建立时间和保持时间均不得短于 200 ns。
- 安全启动顺序：保持 EN1 低电平并停止 STEP，设置 DIR，拉高 EN1，等待至少 1 ms，再启动 STEP。
- EN1 重新拉低会使驱动器休眠并失去保持力矩；闭环滚球工作期间不得无条件休眠。
- PB16 的 TIMG7_C1 路由来自板级和既往 SysConfig 证据；实际固件仍须在当前 `.syscfg` 与重新生成的 `ti_msp_dl_config.h` 中确认实例名和宏名。

### 5.2 步进电机连接

优先使用套件随附的 4 芯带锁线束连接 42 步进电机与 D36A Motor 1/CN1。

| CN1 引脚 | 相线 |
|---|---|
| 1 | B1- |
| 2 | B1+ |
| 3 | A1- |
| 4 | A1+ |

如果重新压线或更换电机：

1. 断电后使用万用表电阻档确认 A、B 两组线圈。
2. 将同一线圈的两端接到同一相的 `+/-`，不得把两组线圈交叉配对。
3. 不用调换线圈来修正控制方向，优先在确认机械安全后标定 DIR1 极性。

电机线色、DIR1 高低电平对应的摆杆升降方向均为 TBD，不得从示例照片推断。

### 5.3 拨码开关设置

D36A 的 DIP 1/2/3 同时设置两路细分，DIP 4/5/6 同时设置两路电流；拨到 `ON` 为导通。

| 拨码 | 当前状态 | 结果 |
|---|---|---|
| 1、2、3 | 全 OFF（已确认） | 16 细分 |
| 4、5、6 | 全 ON | 约 0.55 A，最低电流档 |

当前 42 步进电机型号和额定相电流为 TBD。取得电机铭牌、厂家参数或完成线圈/温升/力矩实测之前，不得提高电流档位。D36A 可设电流的资料上限约为 1.44 A，不等于本电机允许使用 1.44 A。

### 5.4 机械安装

1. 使用 4 颗 M3×7 十字螺丝将 42 步进电机固定到电机支架。
2. 将抱紧式联轴器套入电机轴并锁紧。
3. 使用 3 颗 M3×8 黑色十字圆头螺丝将摇臂固定到联轴器。
4. 使用 M4×16 银色十字圆头螺丝连接摆杆、摇臂和防松螺母。
5. 使用 M4×16 螺丝依次连接不锈钢角码、摆杆和防松螺母。
6. 使用 M4×16 螺丝将不锈钢角码固定到车体板件。

两个 M4 转动关节的防松螺母不得过度拧紧，否则会导致吊臂或摆杆运动受阻。首次运动前应断开钢球、降低 STEP 频率并手动确认全行程无卡滞。

正式 H 题摆杆由长度 25 cm 的 4 分 PPR 水管改造，外径约 2 cm、壁厚约 0.34 cm，且整个摆杆不得超出车身。机构的电机支架、抱紧式联轴器、摇臂、吊臂、角码和尺寸图见 [D36A 与球杆资料索引](.agents/skills/c07a-s28a-car/references/module-docs/d36a-ballrod/index.md)。

### 5.5 已确认的球杆相对位置基准

- 每次上电前人工把球杆调到水平；该上电位置定义为累计步数的相对 `0` 步。
- 当前已确认的安全软行程为 `-238～+238` 微步。它是软件保护范围，不是机械绝对限位。
- 当前控制配置为 `APP_BALL_DIR_INVERT=0`。
- D36A 当前使用 1/16 细分。
- 系统没有角度传感器、编码器和限位开关；累计步数只在电机不失步时代表相对杆角。失步或机构重新装配后必须重新人工调平并上电。
- PB17 高电平对应的实际电机方向和球杆升降方向尚未确认，不作推断。

### 5.6 待标定项目

- 42 步进电机具体型号、额定相电流、相电阻和线圈线色
- DIR1 高/低电平对应的电机方向和摆杆升降方向
- 机械绝对限位、允许最大角度，以及相对软行程之外的安全余量
- 16 细分下的可用 STEP 频率、最大加速度、失步边界和温升
- 7.4 V 电池从满电到低电状态下的力矩、速度和控制稳定性

---

## 6. 八路灰度传感器

传感器接口：

```text
AD0
AD1
AD2
OUT
GND
VCC
```

硬件选通逻辑：

```text
AD2 AD1 AD0
   ->
选择 CH1~CH8
   ->
OUT 输出当前通道数字电平
```

现有拓扑：

```text
AD0 -> PA22
AD1 -> PA8
AD2 -> PA12
OUT -> PA27
GND -> GND
VCC -> 5 V
```

传感器物理方向：

```text
软件 Channel 0 / 模块 CH1 = 小车最左侧
...
软件 Channel 7 / 模块 CH8 = 小车最右侧
```

通道地址表（AD0 为最低位，AD2 为最高位）：

| AD2 | AD1 | AD0 | 选中通道 |
|---|---|---|---|
| 0 | 0 | 0 | CH1 / 软件 Channel 0 |
| 0 | 0 | 1 | CH2 / 软件 Channel 1 |
| 0 | 1 | 0 | CH3 / 软件 Channel 2 |
| 0 | 1 | 1 | CH4 / 软件 Channel 3 |
| 1 | 0 | 0 | CH5 / 软件 Channel 4 |
| 1 | 0 | 1 | CH6 / 软件 Channel 5 |
| 1 | 1 | 0 | CH7 / 软件 Channel 6 |
| 1 | 1 | 1 | CH8 / 软件 Channel 7 |

模块硬件参数：

| 参数 | 已确认事实 |
|---|---|
| 供电 | 5 V |
| OUT 信号 | 数字高/低电平；接至 CD4051 OUT |
| OUT 最大电平 | 3.3 V |
| EN | 板载 10 kΩ 下拉，正常使用无需外部控制 |
| 外形尺寸 | 约 91×32.50 mm |
| 推荐工作高度 | 18 mm |
| 可选发光颜色 | 红、绿、蓝、白；当前安装版本 TBD |

厂家给出的工作高度范围随发光颜色和地面颜色变化：

| 模块版本 | 白色背景 | 黑色背景 |
|---|---|---|
| 白光 | 10~30 mm | 10~30 mm |
| 红/绿/蓝光 | 16~24 mm | 12~20 mm |

仍须确认：

- 当前实物是红、绿、蓝还是白光版本。
- 当前实物与赛道材质组合下：黑线 OUT = 高，白底 OUT = 低（用户实车确认）。
- 地址切换后到读取 OUT 所需的可靠稳定时间。

OUT 已确认为数字信号，PA27 应作为 GPIO 输入使用，不作为灰度模拟量或 ADC 输入。

来源说明：模块硬件和通道选择事实来自 Skill 内打包的厂家资料，见 [灰度模块资料索引](.agents/skills/c07a-s28a-car/references/module-docs/grayscale/index.md)。资料中的其他开发板接线和示例程序不属于本项目硬件事实。

---

## 7. OLED

保留厂家原有硬件资源：

```text
PA28
PA31
PB14
PB15
```

---

## 8. 板载 IO

```text
PA18 -> 独立按键 BLS
PB9 -> 状态 LED
PA9 -> 蜂鸣器（GPIO 输出，高电平触发；低电平关闭）
```

---

## 9. 电源

```text
Battery = 7.4 V nominal
P03B = nominal 5 V / 3 A
```

固定供电拓扑：

```text
7.4 V Battery
|
|-- P03B Logic 5 V
|   |-- MSPM0 / S28A
|   |-- Sensors
|   `-- K230
|
`-- Raw 7.4 V branch
    `-- D36A VIN+ / VIN-
        `-- 42 stepper motor on channel 1
```

- D36A `VIN+` 直接接电池正极，`VIN-` 直接接电池负极，不经过 P03B 5 V 输出。
- D36A 逻辑 GND 与 S28A GND、K230 GND、电池负极全部共地。
- D36A 5 V 引脚是模块输出，本项目不使用，禁止与 P03B 5 V 并联或反向给逻辑系统供电。
- 厂商整板手册和排查流程使用外接 12 V；本项目由用户明确选择 7.4 V 直供，因此不能把厂商的 12 V 波形、转速或力矩表现直接视为本项目实测结果。
- ATD5984 电机电源工作范围和板载 RT8279 降压芯片输入工作范围的资料下限均为 5.5 V。该数值只是器件工作下限，不是电池安全放电阈值。
- 正式上车前须在电池满电、标称电压和低电状态下，测量 D36A VIN、板载 5 V、风扇转动、驱动与电机温升、保持力矩、动态力矩及丢步情况；低电压或负载压降使 D36A VIN 接近 5.5 V 时不得继续宣称稳定运行。

文档检查、芯片范围或空载转动均不能替代上述实车负载验证。
